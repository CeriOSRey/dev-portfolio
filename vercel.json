{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "version": 2,
  "functions": {
    "api/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "builds": [
    { "src": "server/**/*.ts", "use": "@vercel/node" }
  ]
}
